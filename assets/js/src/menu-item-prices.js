/**
 * Allow prices to be added and deleted on the Menu Item editing page
 *
 *  @since 1.5
 */
jQuery( function ( $ ) {
	var $prices = $( '#fdm-input-prices' );

	if ( !$prices.length ) {
	   return;
	}

} );
